//FRAGMENT SHADER
#version 330

in vec2 pass_textureCoords;

out vec4 out_Color;

uniform sampler2D textureSampler;

void main(void) {

	vec4 textureColour = texture(textureSampler,pass_textureCoords);
	
	out_Color = vec4(1,0,0,1);
	out_Color = out_Color * textureColour;
	
}